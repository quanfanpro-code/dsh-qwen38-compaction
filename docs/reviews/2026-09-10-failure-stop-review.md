# 0.1.3 压缩失败停止聊天验收

实现范围：只读现有压缩事件，在原生下一步入口拦截失败或未完成的会话。自动压缩错误向上抛出，不再仅警告后继续。原生手动压缩可用于恢复。不新增依赖、服务商或存储。

验证记录：新增两项测试先在旧实现失败，证明确实存在“失败后发普通请求”和“重开后放行”；新实现下两项通过。全套 8 项通过、0 失败，包括已有请求适配、超时、普通模型及历史保留检查。没有给真实业务会话制造失败，也没有向业务会话发送测试消息。

84% 那次历史调查：2026-09-10 08:46:31 自动压缩启动，08:48:08 结束错误 Request aborted；紧接 turn/end 的原因是 aborted，内部原因 kind=user。DSH 官方代码 apiproxy 的 sessions.cancel 路径调用 agent.cancel({kind:'user'}, {keepInbox:true})；客户端普通会话停止操作调用该接口。该证据证明任务被取消，不能把它归因为模型超时。随后 08:48:14 的手动压缩于 08:53:21 返回 Request timed out，是另一次失败。

归因边界：会话记录不含取消接口的调用者、页面动作或设备标识；当前启动器仅消费进程输出，没有写运行日志。不能从 kind=user 判断是用户亲自点击、快捷键还是自动化调用，不能臆断责任人。

刚才成功记录：09:48:32 至 09:54:59，完成摘要和历史替换，页面占用 84% 降到 48%。此前失败状态不能算成功。

交付状态：测试与构建已通过；安装和重载结果以交付时实际输出为准。未在本轮执行 Git 提交或远端发布。
