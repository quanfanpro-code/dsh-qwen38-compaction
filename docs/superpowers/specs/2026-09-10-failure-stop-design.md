# 压缩失败停止聊天方案

复用会话内已有 compaction/start、compaction/end 记录，在原生下一步入口检查最近一次压缩：失败或未闭合则抛出中文提示，成功则放行。自动压缩本轮失败也抛出错误，不再吞掉错误继续聊天。手动压缩入口不加拦截，以便恢复。

相比仅修改自动压缩 catch，这个方案也覆盖手动失败和重启；相比新增数据库锁，只读现有记录无需维护第二份状态。取消记为未成功，不能绕过。保留原生错误恢复和安全替换。

回滚：安装备份的 0.1.2 包。无需改动业务历史或模型设置。安装后需重载插件；不打断其他正在运行的任务。
