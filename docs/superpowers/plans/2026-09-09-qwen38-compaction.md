# Qwen3.8 压缩插件实施计划

执行方式：本任务串行执行。用户已明确要求直接编写，不再为方案和计划重复等待确认；仍执行测试、审查和文件保护。

目标：交付能在原有 DSH 标准模式中完成自动、手动压缩的插件，并发布到已确认的公开仓库。

结构：固定官方 0.1.2-rc.1 源码，仅对压缩专用模型副本增加思考开关；原生模式配置提供摘要上限。通过官方插件包加载，保留原接入及凭据。

技术：Windows、本机 Node.js、Node 内置测试、DSH 原组件。设计依据：../specs/2026-09-09-qwen38-compaction-design.md。

所有相对文件路径均相对于本项目根目录。

## 任务 1：请求适配与打包

- [x] 在 tests/请求适配.test.mjs 建立真实 HTTP 请求检查，先运行原版适配器，确认摘要请求缺少关闭思考参数而失败。
- [x] 在 src/适配策略.mjs 实现精确目标判断和模型副本；scripts/构建.mjs 从固定官方源生成 dist/adapter.mjs，原代码的 modelOf 结果仅经适配函数处理。
- [x] 请求断言：`assert.equal(body.chat_template_kwargs.enable_thinking, false)`；普通请求、其他目标与原始模型对象不变。并发请求必须相互隔离。
- [x] 运行 `node --test tests/请求适配.test.mjs`，要求全部通过、退出码 0。
- [x] package.json 声明插件包、精确依赖、发布文件白名单；上游 LICENSE 和来源摘要随包交付。

## 任务 2：标准模式与生命周期

- [x] tests/压缩流程.test.mjs 通过真实原生压缩组件触发压缩；在未增加策略时验证输出预算不足的预期失败。
- [x] src/模式入口.mjs 提供保留 standard 标识的原生模式目录封装；生成标准模式副本，仅插入目标摘要预算，其他内容逐项不变。
- [x] cordis.patch.yml 停用原适配器和模式目录入口，并插入插件入口；不依赖把 name 字段当重命名。
- [x] 自动和手动压缩通过原组件执行，断言产生摘要、历史可追溯、继续对话保留预埋事实；取消、空摘要、截断、服务报错不写入损坏摘要。
- [x] 运行 `node --test tests/*.test.mjs`，要求全部通过、退出码 0。

## 任务 3：真实服务与本机安装

- [x] scripts/真实压缩验收.mjs 使用本机已有设置、凭据与合成长对话；输出只含验收数据，不输出真实凭据或地址。
- [x] 在无旧补丁环境中安装实际打包制品；分别完成真实自动压缩和手动压缩，再提问核对数字、约束、纠正和待办。
- [x] 按 BackUp 规则备份现有修改，核对官方原版后仅撤销本项目先前修补；保留用户默认模型和其他插件。
- [x] 安装到现有 web 配置，确认空闲后重启原快捷方式，核对已安装插件及原对话入口。

## 任务 4：审查与发布

- [x] README.md、LICENSE、NOTICE.md 说明中文操作、固定版本、真实验收结果及限制；只公开合成材料。
- [x] 复核实际打包文件、敏感信息、中文编码、测试与构建结果，写入 docs/reviews/2026-09-09-qwen38-compaction-review.md。
- [x] 创建已获授权的公开仓库 quanfanpro-code/dsh-qwen38-compaction，MIT 协议；推送源码及安装附件。
- [x] 核对远端公开性、提交和文件树；最终只报告真实完成项，未成功的验收不标记完成。
