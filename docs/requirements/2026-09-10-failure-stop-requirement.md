# 压缩失败停止聊天

用户已明确要求：压缩失败不能继续对话，并查明 84% 那次自动压缩中止原因。

范围：现有插件覆盖的标准模式。自动和手动压缩失败、取消后停止后续模型聊天调用；允许原生 /compact 重试，成功后恢复。重启不能丢失失败状态。保留原历史，不新增模型接入、依赖、设置项或自动重试循环。

验收：真实 DSH 循环遇到失败时，普通聊天 HTTP 请求为零；恢复失败会话仍被拦截；成功压缩后可聊天。历史原因仅按事件和取消调用链归因，不能把取消说成超时或臆断操作者。

无待确认产品选择；按用户已明确的直接修改、最短路径要求连续执行，不逐项重复确认。
