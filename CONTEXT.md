# DSH 与 Qwen 上下文压缩

本项目用于让现有 DSH 对话在使用 Qwen3.8 时正常完成上下文压缩。

## 术语

**上下文压缩**：把较早对话整理成摘要，让后续对话携带摘要和近期内容；不是删除原始对话记录。

**自动压缩**：DSH 根据对话占用情况主动发起的上下文压缩。

**手动压缩**：用户通过 DSH 原有操作主动发起的上下文压缩。

**摘要请求**：为了压缩而向当前模型发出的总结请求，与普通聊天请求分开。

**模型接入**：用户已经配置的模型、服务地址和凭据的组合；安装本插件不应产生第二个接入。

**标准模式**：DSH 中标识为 standard 的原有对话模式，包括已使用这个模式的历史对话。
